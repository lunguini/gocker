# Gocker docs

Operator- and maintainer-facing notes. Start here when something is confusing that isn't obvious from the Go source.

- [Image channels (`:latest` vs `:dev`)](image-channels.md) — how template images ship, which one gocker picks, CI mapping, release flow.
- [Shared VM internals](shared-vm-internals.md) — the host/in-VM gocker split, compose paths, mount drift, error surfacing, and a fast diagnostic recipe when something fails.
- [Docker CLI compatibility](docker-compatibility.md) — hand-written matrix of which Docker commands/endpoints gocker supports.
- [Docker compatibility matrix](docker-compatibility-matrix.md) — generated by `test/compat/audit.sh`. Do not edit by hand.
- [Apple Container `lost+found` issue](apple-container-lostfound-issue.md) — draft GitHub issue for apple/container about the ext4 volume formatting quirk.

For AI-agent context (conventions, commands, architecture), see `../CLAUDE.md`.
For planned work, see `gh issue list --label roadmap`.
